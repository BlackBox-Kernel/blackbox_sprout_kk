--- drivers/base/regmap/regmap.c
+++ drivers/base/regmap/regmap.c
@@ -600,11 +600,6 @@
 	if (val_bytes == 1) {
 		wval = (void *)val;
 	} else {
-		if (!val_count) {
-			ret = -EINVAL;
-			goto out;
-		}
-
 		wval = kmemdup(val, val_count * val_bytes, GFP_KERNEL);
 		if (!wval) {
 			ret = -ENOMEM;
