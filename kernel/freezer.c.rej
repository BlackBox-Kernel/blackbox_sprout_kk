--- kernel/freezer.c
+++ kernel/freezer.c
@@ -36,9 +36,6 @@
 	if (p->flags & PF_NOFREEZE)
 		return false;
 
-	if (test_thread_flag(TIF_MEMDIE))
-		return false;
-
 	if (pm_nosig_freezing || cgroup_freezing(p))
 		return true;
 
